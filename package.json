{
  "name": "heartsoul-fichas",
  "private": true,
  "description": "Site de fichas/mesa do sistema Heartsoul. Este package.json existe só para os testes automatizados das regras do Firestore (o site em si continua sendo HTML/CSS/JS puro, sem build).",
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore \"node --test tests/\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase-tools": "^13.29.1"
  }
}
